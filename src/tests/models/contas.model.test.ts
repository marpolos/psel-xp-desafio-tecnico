describe('GET contas/{id} - getById', () => {
  test('Quando passa um id válido retorna um objeto com id, nome e saldo', () => {
    expect(2 + 2).toBe(4);
  });
  test('Quando passa um id inválido, um erro é lançado', () => {
  });
});